# MiMo Context Memory — Stage-Path

> Mémoire persistante entre sessions. Ultra-condensée — pas de doublons avec AGENTS.md.

## En cours
- Aucune tâche en cours

## Dernière session (2026-06-18)
- **Header refonte** : tabs (Distribution, Déplacements, Scénographie, Export, Options) déplacés du sidebar vers le header
- Layout : header pleine largeur → workspace (canvas + sidebar) → footer timeline
- `activeSidebarTab` state liftée dans `App.tsx`, partagée entre `HeaderPanel` et `ControlSidebar`
- Bouton `+` dans le header pour ajouter un acteur (couleur aléatoire)
- Bouton toggle sidebar (PanelRightOpen/Close) dans le header
- Formulaire "Nouveau Figurant" supprimé de `CastingTab` (doublon avec bouton +)
- CSS : `.app-header-bar`, `.header-tab`, `.header-tab-active`, `.header-icon-btn`, `.header-add-btn`
- `FooterTimeline` : contrôles zoom et input durée supprimés, waveform prend toute la largeur
- Build : ✅ OK

## Fixes lag (2026-06-18)
- `useMemo(visibleArtists)` — évite recalcul à chaque frame canvas
- `useMemo(formatTime)` — évite recréation closure
- **Supprimé `backdrop-filter: blur()` du header** — cause principale du lag (blur recalculé 60fps quand canvas derrière redessine)
- Grid `auto 1fr auto` au lieu de `64px 1fr 220px`

## Pièges rencontrés
- **CRLF** : les fichiers sont en CRLF. `edit_file` échoue souvent. Utiliser des scripts Python avec `encoding='utf-8'` explicite.
- **Python write sans encoding** : plante sur emojis (👥 etc.). Toujours `encoding='utf-8'`.
- **`git checkout -- src/index.css`** supprime les ajouts jamais commités — attention quand on restaure un fichier.

## Notes rapides
- CSS vanilla, pas de Tailwind compiler — classes utilitaires dans `index.css`
- `StageCanvas.tsx` < 300 lignes, autres composants < 400 lignes
- Zéro `any` TypeScript
- Compétences dans `.memo/skills/` (chargement à la demande, pas tout lire d'un coup)
- Fichiers en CRLF — `edit_file` ne fonctionne pas toujours, utiliser des scripts Python
